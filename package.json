{
  "name": "my-mongodb-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "mongodb": "^3.2.7"
  },
  "scripts":{
    "now-deploy": "now ./out --static --no-clipboard --token=$NOW_TOKEN --public" 
  }
}
